  <div class="score">
    SCORE: <span>empanada</span>
  </div>

  <div class="question-container">
    <div class="question"> rgb(0,0,0)</div>

    <div class="options">
      <div class="option"></div>
      <div class="option"></div>
      <div class="option"></div>
    </div>
  </div>

  <div class="result won">
    <span>You WOnnered like TSM</span>
    <div class="close">
      <a href="#">Siguiente</a>
    </div>
  </div>

  <div class="result lost">
    <span>Hahahaha you lost like clown9</span>
    <h1>Score: <span id="finalScore"></span></h1>
    <div class="close">
      <a href="#">Volver a jugar</a>
    </div>
    <div class="close">
      <a href="#">Registra tu puntaje</a>
    </div>
  </div>

<script>
//declared here for scope purposes.
var correct;
var score = 0;
//initialize a new game
game();
//listener for onclick of the option
$('.option').on('click', guess);
$('.close').on('click', function() {
event.preventDefault();
$('.result').hide();
$('.option').removeClass('scale');
game();
});

function game() {
correct = Math.floor(Math.random() * 3);

$('.option').each(function(index){
  var color = generateColor();
  $(this).css('background-color', color);

  if (index == correct) {
    $('.question').text(color);
  }
});
}

function guess() {
$(this).addClass('scale');
var index = $('.option').index(this);

if (index == correct) {
  score++;
  $('.result.won').show();
} else {
  $('.result.lost').show();
  $('#finalScore').text(score);
  score = 0;
}
$('.score span').text(score);
};

//returns a number between 0 and 255
function random(){
return Math.floor(Math.random() * 255);
};

//returns a color like this rgb(136, 45, 67)
function generateColor(){
return 'rgb('+ random() + ', '+ random() +', '+ random() + ')';
};
</script>
